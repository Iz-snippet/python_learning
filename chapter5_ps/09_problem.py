# can you change  the values inside a list which contain as s
# s={8,7,12,"Ishake",[1,2]}

s={8,7,12,"Ishake",[1,2]}

# ans:-
# This will raise an error:

# TypeError: unhashable type: 'list'

# Sets in Python (set) can only contain immutable (hashable) items.

# Lists are mutable, so they cannot be added to a set.
